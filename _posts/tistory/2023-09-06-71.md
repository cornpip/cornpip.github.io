---
title: "sql JOIN(Inner, Outer, Cross, Self)"
date: 2023-09-06 18:35:08 +0900
categories: [Language, SQL]
tags: ["SQL", "inner join", "outer join"]
render_with_liquid: false
permalink: /posts/71/
lang: ko-KR
alt_lang: en
alt_url: /en/posts/71/
---

> 이 글은 Tistory에서 이전된 포스팅입니다. 원본은 [여기](https://cornpip.tistory.com/71)에서 확인할 수 있습니다.

### **Inner Join**

- 교차 부분만 추출한다. ( NULL이 없다. )

### **Outer Join**

- Left Outer Join \
  - 왼쪽 테이블 기준으로 추출한다. JOIN 조건과 무관하게 왼쪽 데이터가 모두 보이는 결과이다.\
  - JOIN 조건(on)에 맞지 않은 부분은 NULL 로 구성된다.\
  \
- Right Outer Join \
  - 오른쪽 테이블 기준으로 추출한다. JOIN 조건과 무관하게 오른쪽 데이터가 모두 보이는 결과이다.\
  - JOIN 조건(on)에 맞지 않은 부분은 NULL 로 구성된다.\
  \
- Full Outer Join \
  - 양쪽의 모든 데이터를 추출한다. JOIN 조건과 무관하게 양쪽 데이터가 모두 보이는 결과이다.\
  - 데이터 조합을 추출하는건 아니다. ( JOIN 조건이 있다. )\
  - JOIN 조건(on)에 맞지 않은 부분은 NULL 로 구성된다.

### **Cross Join**

- 모든 데이터 조합을 추출한다. ( JOIN조건이 없다. )

### **Self Join**

- 자기 자신에게 별칭을 지정한 후 다시 조인한다.
